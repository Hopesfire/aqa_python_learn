[pytest]
markers =
    smoke: marker for running smoke tests.
    ui: marker for running ui tests.
    api: marker for running api tests.
    playwright: marker for playwright tests.
    selenium: marker for selenium tests.
addopts = -q -ra